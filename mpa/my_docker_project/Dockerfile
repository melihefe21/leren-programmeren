# Use the official PHP image with Apache
FROM php:7.4-apache

# Copy the source files to the /var/www/html directory in the container
COPY src/ /var/www/html/

# Expose port 80 to the outside world
EXPOSE 80

